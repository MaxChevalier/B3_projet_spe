version: '3.4'

services:

  backend:
    image: backend
    build:
      context: .
      dockerfile: DockerFile
    ports:
      - "3000:3000"
    environment:
      NODE_ENV: production
    volumes:
      - .:/Back
      - data:/data/db

volumes:
  data: